[gd_scene format=3 uid="uid://ci2qwmwfgnn4x"]

[sub_resource type="GDScript" id="GDScript_6w285"]
script/source = "extends Node3D

@export var speed = 100.0

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	var movement_input = Vector2.ZERO
	if Input.is_action_pressed(\"Down\"):
		movement_input.y -= 1
	if Input.is_action_pressed(\"up\"):
		movement_input.y += 1
	if Input.is_action_pressed(\"Left\"):
		movement_input.z -= 1
	if Input.is_action_pressed(\"Right\"):
		movement_input.z += 1
	movement_input = movement_input.normalized()
	position = movement_input
	pass
"

[node name="TestScene" type="Node3D" unique_id=1055296890]

[node name="Birb" type="Node3D" parent="." unique_id=1625448702]
script = SubResource("GDScript_6w285")
